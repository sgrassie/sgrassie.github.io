#!/usr/bin/env bash
# Preview the site locally at http://localhost:4000 with live reload.
# Extra arguments are passed to jekyll, e.g. script/serve --drafts
set -euo pipefail
cd "$(dirname "$0")/.."
source script/env.sh
bundle exec jekyll serve --livereload "$@"
